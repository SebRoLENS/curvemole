name: ChatGPT component labels patch

on:
  push:
    branches: [chatgpt/component-labels-remove-curves-20260827]
    paths:
      - ".chatgpt/apply_component_labels_patch.py"
      - ".github/workflows/chatgpt-component-labels.yml"

permissions:
  contents: write

jobs:
  patch-and-test:
    if: ${{ !contains(github.event.head_commit.message, '[patched]') }}
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: chatgpt/component-labels-remove-curves-20260827
          fetch-depth: 0

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            fonts-dejavu-core latexmk \
            libegl1 libgl1 libxkbcommon-x11-0 libxcb-cursor0 \
            pandoc texlive-xetex texlive-latex-extra texlive-fonts-recommended

      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
          enable-cache: true

      - name: Install Python dependencies
        run: uv sync --locked --group dev

      - name: Apply patch
        run: uv run python .chatgpt/apply_component_labels_patch.py

      - name: Rebuild manual
        run: python scripts/build_manual.py

      - name: Validate
        env:
          QT_QPA_PLATFORM: offscreen
          MPLCONFIGDIR: ${{ runner.temp }}/matplotlib
        run: |
          uv run ruff check .
          uv run pytest -q

      - name: Commit validated patch and remove helpers
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          rm -rf .chatgpt
          rm -f .github/workflows/chatgpt-component-labels.yml
          git add -A
          git commit -m "Add curve removal and component plot labels [patched] [skip release]"
          git push origin HEAD:chatgpt/component-labels-remove-curves-20260827
